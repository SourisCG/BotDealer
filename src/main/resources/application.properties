spring.application.name=jarvisdealer
# 2. Configuración de la base de datos H2 (Modo Archivo)
# Esto crea una carpeta llamada "db" en la raíz de tu proyecto con los datos
spring.datasource.url=jdbc:h2:file:./db/apuestas_db;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# 3. Configuración de Hibernate / JPA
# "update" crea las tablas automáticamente si no existen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# 4. Consola de H2 (Opcional, para ver los datos en el navegador)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console